You are choosing the single best webpage for a search query.

Given the query below, use web search to identify relevant results. Then inspect the top 3 candidate pages and determine which one most likely matches the query intent.

Requirements:
- Use web search.
- Visit or inspect the top 3 most relevant pages you find.
- Pick exactly 1 page as the best match for the query.
- Return only JSON that matches the required schema.
- Do not include markdown, explanations, or any extra keys.

Query:
{query}